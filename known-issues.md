# Known issues

Running log of open bugs, rough edges, and recurring failure patterns.
Add when discovered. Remove when resolved.

Format: [date] — [description] — [severity: critical/high/medium/low]

Open:
- *e.g. 2026-03-15 — PDF parser returns empty string on scanned image-only PDFs — high*
- *e.g. 2026-03-15 — No retry logic on AI calls — transient failures show error to user — high*

Resolved:
- *Move items here when fixed, with resolution date*
